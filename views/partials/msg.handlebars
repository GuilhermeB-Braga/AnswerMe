{{#if success_msg}}

    <div class="msgWrapper">

        <div class="msg success">{{success_msg}}</div>
        
    </div>
    
{{/if}}

{{#if error_msg}}

    <div class="msgWrapper">

        <div class="msg error">{{error_msg}}</div>

        
    </div>

{{/if}}